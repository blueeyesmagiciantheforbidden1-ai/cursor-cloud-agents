# Grok cloud return commissioning

Owner: Codex Grok coordinator. This records actual commissioning evidence, not an invented Grok contribution.

The resumed request is to finish the remaining real Codex, Copilot and Grok returns. The Grok task is the existing generic, prompt-only review in room `8c931fe1237a4042a14432677894ee9c`. No project files or other provider credentials enter that prompt.

The approved opaque Grok credential was enrolled for Blueeyes in the existing Google project. The parent coordinator owns all combined broker configuration changes. Broker version 6 preserves earlier bindings and includes separate one-execution capabilities for Grok metadata retry and review. The review uses `runcrew-broker-client-grok-blueeyes-commission1/versions/3`, not a repeated metadata grant.

Two metadata-only cloud executions completed with credential writeback committed and released, zero sessions, zero task claims and zero model calls. The first (`runcrew-worker-grok-blueeyes-hjcrh`, UID `6a684fa2-2ce0-47b6-968f-f6d0c535b130`) exposed the missing underscore prefix on ACP extension methods. The second (`runcrew-worker-grok-blueeyes-rx7ch`, UID `202433d2-1919-4c0e-a06c-1f252397751b`) exposed the nested `ExtMethodResult.result` envelope for model-list responses. Both are preserved as failed preflight evidence, not successful eligibility checks.

The corrections follow the official xAI Grok Build Rust source. Authentication and billing return direct objects; model-list returns the typed nested result/error envelope. The final wrapper refuses provider errors, unknown billing percentages, expired periods, malformed auto-top-up payloads and unknown extra-credit balances. A fresh native authenticated identity/catalog/billing check and selected model/effort round-trip occur in the same process before any hub claim or provider prompt.

The bounded review pins `grok-4.7` and requires its actual advertised maximum effort to be `xhigh`. If the account does not expose this model and effort, it stops before inference. No lower model or paid API substitute is silently selected. The native environment contains no inherited API key or hub token. Native tools are denied and observed tool requests abort; this is a deny-and-monitor policy, not a claim that ACP exposes an independently verified empty tool catalog. The wrapper sends one prompt; uncertain inference is never replayed automatically.

Final immutable source snapshot: `WORK/cloud-agent-online/grok-review-20260921b-source`. Cloud Build `51ba8df5-280f-4f57-b1e7-a5b827e7008a` succeeded, including Linux tests and native immutable-image checks. Image digest: `grok-worker@sha256:ac9eea6c2e9dd079101c421f4f2fd0ef856114d545187564f3e9bfa5206ce6a3`. Twenty-nine runtime tests and four separate controller tests passed. The earlier review image a was not deployed or executed.

Existing dedicated Grok hub-token secret access and hub invoker permission were verified read-only; no permission changes were needed. The parent independently refreshed browser billing at 22:01:28 UTC, showing intended Blueeyes identity, SuperGrok Heavy, 0% Heavy usage, zero extra credits and auto-top-up off. This corroboration does not replace the mandatory fresh native check.

Controller: `WORK/commission_grok_review_job.py`; exact current journal: `WORK/cloud-agent-online/grok-review1-job.json`. The job update/launch is underway at this checkpoint. No final Grok answer is asserted here. Results, if completed, will be recorded in `WORK/cloud-agent-online/grok-review1-result.json` and the actual hub room. Current helpers and maintained wrapper source are under `WORK/grok-cloud-runtime`; do not rebuild from the older metadata-only SOURCE package without the frozen overlay.

These are bounded Cloud Run Jobs. Five persistent continuously available workers have not been commissioned by this subtask.

## Billing compatibility successor

Review execution `runcrew-worker-grok-blueeyes-s4t86`, UID `0190834f-5bbc-4141-8853-11c2a696aeff`, terminated at 22:12:02.593997 UTC with `billing_percent_missing`. It made zero model calls and zero claims, and committed/released its credential. The original room is still eligible for its first real prompt; this failure is not an uncertain provider answer.

The official billing schema explicitly supports legacy `monthlyLimit`, `used`, `billingPeriodStart` and `billingPeriodEnd`. The successor derives usage only from actual nonzero limit and explicit used cents, and still validates the native current period/cap. It never treats a missing used value as zero. For the documented legacy absence of `prepaidBalance` only, the parent approved an immutable same-owner browser receipt for zero extra credits and disabled auto-top-up, with a maximum age of 15 minutes checked again before claim. Malformed, negative, conflicting or expired data is rejected. Browser data does not replace native identity, usage, period or model evidence.

Fresh browser observation `WORK/grok-browser-evidence-review2-20260921.json`: 22:15:39 UTC, intended Blueeyes, Heavy 0%, Grok Bot 2%, zero extra credits and auto-top-up off. Final image c freezes that full evidence and records its canonical hash when used. Its actual cloud build `3f5f5af6-6cd7-4b0e-ae09-bb9ffd63ea41` succeeded; digest `grok-worker@sha256:667c03266feaddadaac000bcb37eebe2a99f4f4351030af5284dc4315f5aaccb`. Thirty-one runtime tests and three successor-controller tests passed.

The exact job patch to image c and protected client version 4 is verified. The new client receipt is `WORK/cloud-agent-online/grok-review2-client.json`; root verified its exact binding in broker version 8 while preserving all previous bindings. The successor journal is `WORK/cloud-agent-online/grok-review2-job.json` and result path is `WORK/cloud-agent-online/grok-review2-result.json`. The successor launched after a verified 22:24:15 UTC execution intent, before the 22:25:39 launch cutoff: `runcrew-worker-grok-blueeyes-zlfxf`, UID `5abe2837-2a91-4237-bca1-0a3669ed5bd1`. Exact grant publication is verified; actual result is pending at this checkpoint. Preserve every earlier journal and result; do not reuse old grants.

The parent independently reviewed the official auto-top-up omission contract: `extensions/billing.rs` documents proto3 false omission, applies `#[serde(default)]` to `enabled` and tests the omitted field as false. This documented native decoding is distinct from the browser fallback. The immutable image's browser balance is literal integer zero; the launch controller additionally verifies exact numeric type and every frozen source hash. Maintained runtime source now also rejects boolean `False` as a numeric zero, with a passing regression, without changing the already frozen image c.

## Final manual commissioning route (supersedes the legacy browser prepaid route)

Review2 `zlfxf` completed at 22:29:03.519460 UTC with `billing_percent_missing`, credential writeback committed, and no claim or model prompt. Native owner and eligible Grok 4.7 catalog were verified, but the modern native billing response omitted its optional percentage. Official `Option<f64>` semantics do not justify interpreting that absence as zero. The full official schema audit is `WORK/cloud-agent-online/grok-billing-schema-audit.md`.

The parent authorized only this exact manual room and prompt hash to use a fresh, immutable, same-owner browser observation for included usage. Native percentage remains unavailable; automatic improvement remains ineligible on unknown usage. Both browser allowances must be numeric and below 100%, native cap and native prepaid must both be exactly zero, top-up must be disabled, and native identity/model/effort/current period must pass. Missing native prepaid is now rejected even on legacy responses. No browser prepaid substitution survives in the final runtime. Missing optional native plan may be corroborated by the browser plan, with provenance explicit; contradictions fail.

The final frozen proof is `WORK/grok-browser-evidence-review3-final-20260921.json`, observed 22:45:42 UTC: intended Blueeyes owner, SuperGrok Heavy, Heavy 0%, Grok Bot 2%, extra credits zero and top-up off. Its age is checked before launch and again before claim; the final recheck records `checked_at` and verifies the native period at that same time. The completion verifier binds the exact frozen hash, both meters, owner, room, prompt and mutually exclusive native/manual provenance. Delayed polling does not invalidate evidence that was fresh at the actual check.

The native process constructor now cleans up a successfully spawned process if reader startup fails. Credential commit requires explicit confirmed stop. Unknown constructor failures and failed process stop remain quarantined, never assumed stopped. Focused failure tests cover both constructor and post-assignment cleanup. Selected models with unknown reasoning levels are rejected instead of silently asserting a known level is strongest.

Actual immutable image f is `grok-worker@sha256:b2d5950e29e3d6239a9525b77588cb1132fb11426fca5ccb320d002ed6693490`, Cloud Build `54a06de2-ee12-46d2-bb7b-190dd9a7cfda`, verified successful. Its 40 tests pass; seven controller tests and two final recheck tests pass. An independent coordinator reviewed the final scoped corrections. Images d and e were built but never deployed or executed.

The existing job template is verified on f and protected client version 5; parent verified broker version 9 with all prior bindings preserved. After explicit GO, a single execution was started at 22:53:14 UTC: `runcrew-worker-grok-blueeyes-rbwwb`, UID `9eac6baa-0b67-4e22-8fac-32908b0f186d`. Exact grant publication is verified. Journal: `WORK/cloud-agent-online/grok-review3-job.json`; final result target: `WORK/cloud-agent-online/grok-review3-result.json`. At this checkpoint the actual native result remains pending. There is no authorization to replay an uncertain inference, and none has been replayed.

## Actual terminal outcome

Execution `rbwwb` terminated at 22:56:01.559856 UTC. Native preflight passed, including intended authenticated owner, actual eligible Grok 4.7 and xhigh selection, native zero cap/prepaid, disabled top-up and fresh manually authorized browser usage. The final evidence check was 22:55:54.627519 UTC, inside the 15-minute window; the native period was valid through 2026-09-28. Native percentage remained explicitly unavailable. The task was claimed once and one prompt was attempted.

The transport then rejected `unexpected_response`; **no verified Grok answer exists**. This code can mean missing ID, differently typed ID, or mismatched ID in a non-method object. The retained receipt has no raw envelope, so a specific correlation defect cannot be asserted. The provider's actual token usage, charge and completion outcome are unknown. The hub room has one failed attempt with a worker error. No uncertain prompt was replayed.

`WORK/cloud-agent-online/grok-review3-result.json` is the immutable sanitized failure receipt. `WORK/cloud-agent-online/grok-review3-live.json` independently verifies at 23:00:21 UTC that the same execution is terminal with no running tasks, and its durable credential state is idle at fence 5, credential version 2 equals last-release version 2, last-release fence is 5, and no quarantine reason exists. Credential payloads were not accessed during this verification. The immutable runtime requires native close before commit, and Google confirms terminal execution.

Maintained source now adds fixed envelope key/type/correlation diagnostics without raw content or IDs, and refuses boolean IDs masquerading as integers. Two new focused tests pass. These changes are **not** in already-deployed immutable f; no replacement image has been built or launched. A successor must use a new image/version and new capability only after explicit reconciliation. Do not run `start` against the consumed v5 grant. A read-only official session-list/history route may help if the exact existing remote session can be identified; this receipt did not retain its session ID and the container's local session files were ephemeral.

The parent subsequently checked the existing signed-in Grok Chrome history read-only: the newest entry was 26 hours old, and a focused search for commissioning returned only that historical conversation. No exact current answer was found, no prompt was submitted and no session resumed. The final local status capsule is `WORK/cloud-agent-online/grok-final-status.json`, composed from the retained failure and durable-release receipts. All 28 focused native tests pass after the undeployed diagnostic correction.

## User-requested mini tests and distinct cloud diagnostic

The user subsequently explicitly requested small tests around the Grok failure. Nine offline response-matrix tests reproduce missing/null/string/wrong/bool correlation IDs and non-RPC JSON objects through the actual NDJSON reader and request loop. Valid numeric IDs and official method-bearing setup/prompt-complete notifications succeed; malformed JSON fails earlier. The lost `rbwwb` envelope still cannot be inferred. Root added short-write completion handling, a random caller prompt nonce, final-response nonce matching, and filtering of text chunks by session/prompt identity with replay exclusion. Root and independent reviewers verified the source-backed metadata contract and negative cases.

The parent separately authorized exactly one new, tiny diagnostic; it does not replay the failed design-review room. New room `3c2ecb24008f42c6b0395664a8707256` asks for one sentence of no more than 40 words about preventing duplicate model requests after a connection failure. Prompt SHA256 is `2e55d710c8f45a351f21b8a390e68a7d8d8d9e13fcd3d4028496dee00f13e07f`. The new wrapper binds only this immutable room/prompt, keeps a 90-second native limit within the 120-second hub task, and enforces output word count and nonce correlation before completing it.

Mini image `grok-worker@sha256:73c1ecdb99cf942e378adcd99d5e3f8ef299059928198daf6bb711f8c94062c8` was verified by Cloud Build `1becddbf-ad3a-46c0-8660-347124ea704a`. Seventy-five frozen tests and five controller tests pass. An independent reviewer cleared the image, entrypoint and final controller. Protected client version 6 and root-verified live broker version 10 contain the exact new binding, preserving earlier bindings. The frozen actual browser proof was observed at 23:19:23 UTC, Heavy 0%, Bot 2%, zero credits and top-up off; launch cutoff is 23:29:23, pre-prompt validity ends 23:34:23.

After explicit GO and exact template verification, a single new launch intent was recorded at 23:28:00 UTC. Execution `runcrew-worker-grok-blueeyes-kn4l8`, UID `34471211-7cd4-4b62-8090-8f06dfc55b3e`, has exact grant publication verified. The existing Grok job has one task, no retries and a 300-second container limit. Current journal: `WORK/cloud-agent-online/grok-mini-job.json`; result target: `WORK/cloud-agent-online/grok-mini-result.json`; read-only release helper: `WORK/grok_mini_live_readonly.py`. At this checkpoint this distinct test is pending. Old `rbwwb` remains failed and unreplayed.

The mini test completed at 23:31:59.698205 UTC after one claim and one prompt. Its safe diagnostic establishes a concrete response-ID type mismatch: the decoded no-method JSON-RPC object had only `id` (string), `jsonrpc` (string), and `result` (object); the request expected an integer. The actual string value is not retained, so neither numeric-string equality nor prompt-UUID equality is asserted. This finding applies to `kn4l8`; the earlier `rbwwb` envelope remains unavailable. Preflight passed with Grok 4.7/xhigh and zero native paid-spend controls; browser evidence was freshly checked at 23:31:51.414744 UTC. The wrapper stopped and committed credentials, with no verified answer and unknown actual token/charge outcome. No replay occurred.

The immutable sanitized mini receipt is `WORK/cloud-agent-online/grok-mini-result.json`, SHA256 `af13702d40fa98a629d69d330039a2888b440689d0c04da82907aa95a16daa33`. Root is repairing outgoing IDs to use strings consistently while preserving strict response type/value equality. That change is not in the already-finished mini image. Any confirmation must be a separately authorized fresh tiny task with a new capability and fresh evidence; neither prior room may be replayed.

## Distinct string-ID confirmation

The first mini execution's independent terminal/release receipt, `WORK/cloud-agent-online/grok-mini-live.json`, confirms idle state at fence 6 with credential version 2 matching its last release and the exact `kn4l8` execution UID. No native process remains running. The parent then authorized exactly one separate string-ID confirmation. Root changed every outgoing JSON-RPC ID to a string while retaining strict returned type and value checks. New diagnostics distinguish a returned string matching the caller's prompt nonce without retaining either ID. The prior failures remain immutable and are not retried.

The new room is `4a1d5e11276649d2a5ac9b7444c20b51`, prompt SHA256 `fc59f6b2f05510f6c6f13ac33aabc1e098a5d71669cc0b4d82ce0d248c05577e`. It asks for one sentence of no more than 40 words explaining why replies should match their original request ID. The native limit remains 90 seconds, hub room 120 seconds, and job 300 seconds, with one task, one prompt and no retries, tools or paid fallback.

The immutable mini2 image is `grok-worker@sha256:591afa2f99b9ba5ae6d686bc52090b5ee92f3f18219d2a2455d81bbf455ee5ae`, built successfully by `038c0097-e34f-4d89-80c0-3d7db983f2d1`. Seventy-eight frozen tests and five separately run controller tests pass. The parent and independent reviewer verified all 27 frozen file hashes, exact prompt, entrypoint and string-ID fix. Fresh actual browser evidence was observed at 23:41:49 UTC with Heavy 0%, Grok Bot 2%, zero credits and top-up off. The launch deadline was 23:51:49 and the runtime freshness deadline is 23:56:49. Native identity, model, effort, period and zero native paid-spend controls remain mandatory.

Protected client version 7 is bound in parent-verified live broker version 11, preserving all prior 16 bindings. After exact template verification and explicit parent GO, a single launch intent was recorded at 23:50:35 UTC. Execution `runcrew-worker-grok-blueeyes-rkmgn`, UID `12605a55-7b29-4935-b980-6203ec314777`, was created at 23:50:38.268742 UTC and exact grant publication is verified. Journal: `WORK/cloud-agent-online/grok-mini2-job.json`; sanitized result target: `WORK/cloud-agent-online/grok-mini2-result.json`; durable-release target: `WORK/cloud-agent-online/grok-mini2-live.json`. At this checkpoint the execution is pending; no successful answer is asserted.

The string-ID confirmation terminated at 23:53:58.847758 UTC with `unexpected_response`, after exactly one claim and one prompt. Its retained safe envelope proves that both expected and observed outer IDs were strings, but their values differed. The observed ID also differed from the caller's prompt nonce. The only envelope fields were `id` (string), `jsonrpc` (string), and `result` (object). Therefore changing the outbound ID type alone did not repair correlation. The actual returned ID or result payload was not retained, so its origin is still unknown; do not assert it was a prior response, a new server-generated ID, or a successful answer.

Preflight passed with actual Grok 4.7/xhigh, intended native owner, native zero cap/prepaid and top-up off. Exact manually authorized browser usage remained Heavy 0%/Bot 2%, native usage explicitly unavailable. The final check at 23:53:50.785761 UTC was fresh within the native period. The sanitized failure receipt is `WORK/cloud-agent-online/grok-mini2-result.json`, SHA256 `27a3560ed9a9c17c9faa318e09f2f37890d0a45e88151d6ec23a7e511fa35e8e`. There is no verified Grok answer; actual provider token usage, charge and completion outcome remain unknown.

Independent read-only proof at 23:54:56.131195 UTC confirms the exact `rkmgn` UID is terminal with running count 0, succeeded count 0 and failed count 1. Durable credential state is idle at fence 7, last release fence 7, with matching current/last-release canonical credential version 2 paths and no quarantine reason. This proof is `WORK/cloud-agent-online/grok-mini2-live.json`; credential payloads were not read. The native wrapper confirmed close before credential commit. No further launch is authorized and none of the three uncertain provider attempts has been replayed. The next diagnostic must narrow the retained protocol discrepancy locally or by read-only inspection before any separately authorized cloud test.

## Distinct internal-acknowledgement confirmation, 2026-09-22

Independent official-source review identified internal skills/workflows watcher requests in the `--no-leader` stdio stream. Their fixed string IDs can produce background acknowledgements alongside ordinary client replies. Root implemented and independently reviewed exact ID/envelope/result-shape validation for these two internal acknowledgements, with a total limit of eight. Unknown IDs, malformed known acknowledgements and all server tool requests still fail closed. Safe per-kind acknowledgement counters are retained before native close. The lost IDs of the prior attempts remain unknown; source plausibility alone does not prove that those old frames were watcher replies.

The separately authorized new tiny room is `48545bcffe9043ca845f49f8a429b3b6`, exact prompt SHA256 `4b68b6dd07bc47911cc60695870a4a50629acb28aa472e4ad9ef6a081c9d73f7`. It keeps the one-sentence/40-word, 90-second native, 120-second hub and 300-second job limits, with one prompt and no retries, tools or API fallback. The three earlier failed rooms are unchanged.

Actual browser evidence was refreshed at 00:02:21 UTC: intended Blueeyes Heavy account, Heavy 0%, Grok Bot 2%, zero extra credits, top-up off. Launch cutoff was 00:12:21 and pre-prompt expiry is 00:17:21. The immutable image is `grok-worker@sha256:5c874872ffcded49636c5e0261cad47616aca85510d6a3c7d37ef452d6d665ed`, Cloud Build `9c24f050-88e4-4fc2-9b36-229dfb3a3f01`, verified successful. Ninety-one frozen package tests and six controller tests pass; an independent reviewer verified all 28 frozen files. The controller also verifies exact known acknowledgement keys and bounded nonboolean integer counters.

One template precheck refused local test bytecode files; all 28 original hashes were verified unchanged, only generated cache extras were removed, and the manifest was restored exactly. That attempt stopped before any cloud mutation intent. The explicit reconciliation receipt is `WORK/cloud-agent-online/grok-mini3-test-cache-reconciliation.json`; the immutable cloud image was not rebuilt or altered. Future local test commands use Python's `-B` option.

Protected client version 8 is bound in root-verified live broker version 12, preserving all 17 prior bindings. After exact template verification and explicit root GO, a single launch intent was recorded at 00:09:35 UTC. Execution `runcrew-worker-grok-blueeyes-bqcwf`, UID `c4be8773-b4d1-493d-aec0-f7071a827414`, has exact grant publication verified. Journal: `WORK/cloud-agent-online/grok-mini3-job.json`; result target: `WORK/cloud-agent-online/grok-mini3-result.json`; release target: `WORK/cloud-agent-online/grok-mini3-live.json`. At this checkpoint the actual result is pending; no new successful answer is asserted.

An unused standalone passive observer draft exists at `WORK/grok-cloud-runtime/native_passive_observer.py`. It was prepared locally, left untested when direction changed, and explicitly excluded from this image. It has no cloud entry point or grant and must not be used as verified runtime.

### Actual successful Grok return

Execution `bqcwf` completed successfully at 00:12:35.159095 UTC. The controller verified the exact room/prompt, final-response nonce, same-session non-replay answer chunks, model Grok 4.7 and effort xhigh. The native process observed exactly one valid `skills-reload` acknowledgement and kept it separate from the user result. This is direct evidence that the internal acknowledgement occurs on this connection and that the reviewed handling worked in this run; it does not recover the lost IDs from prior failed runs.

The actual 17-word answer, independently read by root from the completed hub room, is: “A background maintenance acknowledgment only confirms system upkeep finished; it is not the user's requested task result.” Its SHA256 is `22e9c82e93dbba28d5abd3cd6f926a22eeed6c080a2ca1616841b15859749f17`. Sanitized result receipt `WORK/cloud-agent-online/grok-mini3-result.json` has SHA256 `78e90fc69a0ae9ee89b154b1fba0bd05f11eff504a8b8aada8b6845af55f56ed`.

The provider reported 15,263 input tokens, 87 output tokens, 67 reasoning tokens and 1,536 cached-read tokens. These are separate reported fields; no additional totals or dollar charge are inferred. Actual charge remains unverified. The native owner, current period and zero cap/prepaid/top-up controls passed. Native included-usage percentage remained unavailable; this exact manual commissioning used the approved Heavy 0%/Bot 2% browser evidence, checked at 00:12:25.445434 UTC. It is not automatic-improvement eligibility proof.

One prompt was sent, no automatic retry or old-room replay occurred, and the native process was confirmed stopped before credential commit. The independent durable-idle verification is in progress at this checkpoint. This tiny diagnostic success verifies the repaired Grok return path; it does not complete the original longer review or commission continuously online workers.

Final independent read-only proof `WORK/cloud-agent-online/grok-mini3-live.json`, observed 00:14:25.313033 UTC, verifies the exact successful execution UID as terminal with running count 0, succeeded count 1 and failed count 0. Durable credential state is idle at fence 8, last release fence 8, with current and last-release canonical credential version 2 identical, the exact execution identity, and no quarantine reason. No credential payload was read, no mutation was made, and no further inference was launched during this verification. The Grok tiny return and clean release are now fully verified.
