# Prompt to paste into Codex

Continue the implementation in this extracted `ryan-frontier` folder. Start by reading AGENTS.md, START_HERE.md, docs/VALIDATION.md, docs/FRONTIER_20.md, and docs/DEPLOYMENT.md. Run `python3 scripts/run_checks.py` and report the actual result.

My goal is a sophisticated self-improving coding system with twenty substantive parts spanning frontier research, rigorous mathematics, and buildable infrastructure. Keep improving against concrete acceptance criteria; fix real failures and record before/after evidence. Do not substitute a larger plan for executable progress or claim an undefined 10/10.

Preserve this package's finite-domain reference implementation and historical experiments. The next useful milestone is integration with my existing command-center repository and two VPS workers. First establish which repository, host connections, and supported coding-agent interfaces are actually available. If they are available, work in an isolated branch/worktree and implement an authenticated controller/worker transport around the existing ledger, then one independently verified repository-repair task. If they are not available, identify the exact missing connection and continue any useful local preparation without fabricating access.

Use my supported Grok, Cursor, Claude, and GitHub Copilot integrations where available, with verified strongest models and highest effort. Conserve the ChatGPT connection allowance. Keep subscription allowances separate from API billing and never assume Alibaba Gateway merges them. Do not print API keys.

A change counts as an improvement when independent checks show better correctness, accepted-task performance, or cost at matched quality, with no unreported regressions. Freeze the evaluation protocol before final validation; reuse failed validation only as development evidence and create a new confirmation set for a revised candidate. Keep a strong simple baseline and full accounting. Deliver tested code, exact evidence, known limits, and the next concrete integration boundary.
